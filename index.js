import React from 'react'; // Importing the React library
import ReactDOM from 'react-dom'; // Importing the ReactDOM library

import App from './App'; // Importing the App component from the './App' file

ReactDOM.render(<App />, document.getElementById('root')); // Rendering the App component inside the element with id 'root'
